# ===================================
            # BASE DE DONNEES
            # ===================================
            DB_USER=postgres
            DB_PASSWORD=postgres
            DB_NAME=event_location_db
            DB_PORT=5432

            # ===================================
            # APPLICATION
            # ===================================
            APP_PORT=8080
            APP_BASE_URL=http://localhost:8080

            # ===================================
            # JWT
            # ===================================
            JWT_SECRET=votre-cle-secrete-tres-longue-ici-avec-au-moins-64-caracteres

            # ===================================
            # EMAIL - CONFIGURATION (STANDARD SPRING)
            # ===================================
            SPRING_MAIL_HOST=smtp.gmail.com
            SPRING_MAIL_PORT=587
            SPRING_MAIL_USERNAME=votre.email@gmail.com
            SPRING_MAIL_PASSWORD=votre-mot-de-passe-application

            # Expéditeur (propriétés applicatives)
            APP_EMAIL_FROM=noreply@votre-domaine.com
            APP_EMAIL_FROM_NAME=Votre Application

            # ===================================
            # (Optionnel) Compatibilité anciennes variables
            # ===================================
            # MAIL_HOST=smtp.gmail.com
            # MAIL_PORT=587
            # MAIL_USERNAME=votre.email@gmail.com
            # MAIL_PASSWORD=votre-mot-de-passe-application
            # MAIL_FROM=noreply@votre-domaine.com
            # MAIL_FROM_NAME=Votre Application